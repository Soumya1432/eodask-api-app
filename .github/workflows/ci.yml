name: CI/CD

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x, 22.x]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npm run db:generate
        env:
          DATABASE_URL: "postgresql://user:password@localhost:5432/dummy"

      - name: Build application
        run: npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        if: matrix.node-version == '20.x' && github.ref == 'refs/heads/main'
        with:
          name: dist
          path: dist
          retention-days: 7

  # Database integration tests
  # test:
  #   name: Test
  #   runs-on: ubuntu-latest
  #   needs: build
  #
  #   services:
  #     postgres:
  #       image: postgres:16
  #       env:
  #         POSTGRES_USER: postgres
  #         POSTGRES_PASSWORD: postgres
  #         POSTGRES_DB: onething_test
  #       ports:
  #         - 5432:5432
  #       options: >-
  #         --health-cmd pg_isready
  #         --health-interval 10s
  #         --health-timeout 5s
  #         --health-retries 5
  #
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 20.x
  #         cache: 'npm'
  #
  #     - name: Install dependencies
  #       run: npm ci
  #
  #     - name: Generate Prisma Client
  #       run: npm run db:generate
  #
  #     - name: Run database migrations
  #       run: npm run db:push
  #       env:
  #         DATABASE_URL: postgresql://postgres:postgres@localhost:5432/onething_test
  #
  #     - name: Run tests
  #       run: npm test
  #       env:
  #         DATABASE_URL: postgresql://postgres:postgres@localhost:5432/onething_test
  #         JWT_SECRET: test-secret
  #         JWT_REFRESH_SECRET: test-refresh-secret

  # Deploy to Railway/Render/Fly.io (example for Railway)
  # deploy:
  #   name: Deploy
  #   runs-on: ubuntu-latest
  #   needs: build
  #   if: github.ref == 'refs/heads/main'
  #
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #
  #     - name: Install Railway CLI
  #       run: npm install -g @railway/cli
  #
  #     - name: Deploy to Railway
  #       run: railway up
  #       env:
  #         RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
